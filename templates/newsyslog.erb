# Fluentd音声文字起こしシステムのログローテーション設定
# Path                                                Owner   Mode  Count  Size  When  Flags  [PID file]
<%= File.join(LOG_DIR, "audio_transcription.*.log") %>  <%= ENV['USER'] %>  644  5  10000  $D0  J
