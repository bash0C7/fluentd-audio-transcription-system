# 2026-05-06 Core feature E5 reverify — observation log

> Real 15-min meeting reverify of the spec
> `docs/superpowers/specs/2026-05-06-core-feature-completion.md`. The capture
> path (mic + screen → SQLite) worked. The Web display layer did not. Three
> runtime defects in the PicoRuby:wasm frontend were diagnosed via
> chrome-mcp, fixed, and verified end-to-end with live YouTube audio.

---

## 1. 15-min real-meeting capture run

Baseline before the run:

```
mic transcripts (ended_at>0):     36
screen transcripts (ended_at>0):   8
state.jsonl rotated events:       50
state.jsonl channel_failed:        0
```

After 15 minutes of speech + system audio (full `rake start:all` →
`rake stop:all`):

```
mic transcripts:                  51   (+15)
screen transcripts:              121   (+113)
audio_segments duration_sec>0:    40
rotated events:                   60   (+10, normal auto-rotate)
channel_failed events:             0   (G1 path not exercised)
leftover pids / processes:         0
```

SCStream did **not** error out during this run, so the G1 mic-only-degrade
runtime path was not exercised observationally — its three unit tests
(`CaptureCoordinatorChannelFailureTests`) carry the load instead. Both
channels produced new transcripts; the spool/SQLite half of the core
feature is healthy.

### Acceptable, non-blocking observation: foundation_model guardrails

`tmp/log/fluentd.log` recorded **9 `[warn]`** lines, all of one shape:

```
error_class=AppleFoundationModel::GenerationError
error="guardrailViolation(..., debugDescription: \"Response may contain
sensitive or unsafe content\", ...)"
```

These come from the `filter_foundation_model_mac` polish step refusing to
process some screen transcripts that Apple's on-device model judged as
sensitive. The raw text is still inserted into SQLite (the 113 screen
delta above includes them); only the polished_text column is missing
for those rows. User has accepted this as expected upstream behavior;
out of scope for this spec.

---

## 2. Web display was the real gap

The user opened http://localhost:9292/ during the run and saw nothing live.
rack-test (Task 5) covered the routes in isolation but never the wired
pipeline: `fluentd → POST /_internal/notify → WEBSOCKETS broadcast →
browser WebSocket → DOM render`. chrome-mcp was used to inspect the
running page directly.

### Defect 1 — three.js double-load crashed `ForceGraph3D` init

`web/views/index.erb` loaded `three@0.160.0` standalone and then
`3d-force-graph` (which UMD-bundles its own three.js).
Console captured:

```
[WARNING] THREE.WARNING: Multiple instances of Three.js being imported.
[EXCEPTION] Uncaught TypeError: Ak.Timer is not a constructor
[ERROR] NoMethodError: undefined method 'new' for NilClass  (picoruby)
```

Because `JS.global[:ForceGraph3D]` stayed undefined, the very first line
of `app.rb` that touched it (`...new.call(getElementById('graph-canvas'))`)
threw, and **every subsequent line** (WebSocket open, /api/recent fetch)
never ran. The static HTML rendered Quick / Perfect / Graph headings only.

**Fix:** removed the standalone three.js `<script>` tag — the bundled copy
inside 3d-force-graph is sufficient.

### Defect 2 — even with three deduped, 3d-force-graph CDN init still throws

After removing the duplicate three load:

```
[ERROR] TypeError: Cannot set properties of undefined (setting 'innerHTML')
        at g.init (3d-force-graph)
[ERROR] NoMethodError: undefined method 'nodeAutoColorBy' for NilClass
```

This is an issue inside the current 3d-force-graph CDN bundle's
container-init logic, independent of how it's invoked from PicoRuby.
The graph panel is *visualization on top of* the core "Web 表示" feature,
not the feature itself; transcripts must render whether or not the graph
works.

**Fix (deferred resolution):** disabled the graph init — `GRAPH = nil` —
and gated `redraw_graph` on it. `app.rb` proceeds past the graph block,
opens the WebSocket, fetches `/api/recent`. **TODO** pinned in the source:
find a working three.js + 3d-force-graph version pair and re-enable.

### Defect 3 — PicoRuby JS bridge mismatches

Two separate API mismatches blocked all data flow even after defect 2 was
gated:

- `JS.global.fetch(url).then { |resp| ... }` raised
  `ArgumentError: JS::Object#fetch requires a block: use 'fetch(url) { |resp| ... }'`.
  PicoRuby's JS bridge shims `fetch` to take a block and resolve the
  Promise internally. **Fix:** use the block form. Note `Response#json()`
  is *not* shimmed — it still returns a JS Promise, so the inner call
  needs `resp.json.then do |data| ... end`.

- The WebSocket message handler used
  `JSON.parse(...).to_a` followed by `msg.find { |kv| kv[0].to_s == 'type' }`.
  Even though messages were arriving (chrome-mcp logged 194 events
  including `{"type":"quick","data":{...}}` from real screen audio),
  `find` consistently returned nil and the handler skipped via
  `next unless type`. **Fix:** direct property access — `parsed[:type]` /
  `parsed[:data]` — works correctly through the bridge.

---

## 3. Verification with live audio

After the three fixes, re-ran `rake start:caffeinate start:swiftcap
start:fluentd` (web was already up) with YouTube playing for ~50s,
inspected via chrome-mcp:

```
WS open, readyState=1
194+ "WS message received" events flowing in
DOM after live capture:
  perfect-stream:  200 entries  (172 bootstrap + 28 live finals)
  quick-stream:     60 entries  (live volatile transcripts)
  perfect_last:    "やんちゃハウスはいに到着しますね。…"  (real final)
  quick_last:      "はい、私もなんかクラフトビールを 1リットル…"  (real volatile)
```

The full pipeline `mic + screen audio → SpeechAnalyzer → spool/*.jsonl →
fluentd filters → SQLite + webhook POST → /_internal/notify → WebSocket
broadcast → PicoRuby:wasm DOM render` was confirmed working
end-to-end.

---

## 4. Test-suite flake (unrelated but caught here)

`test_stop_via_pidfile_aborts_when_term_ignored` flaked when run inside
the full `rake test` suite (passed when run alone). Root cause: the
fixture reuses fixed pidfile names (`lifecycle-test.pid`, `stubborn.pid`)
and a leftover from a prior run lets the
`20.times { break if File.exist?(pidfile); ... }` wait return on
iteration 0 — before the freshly-spawned child has written its own pid.
`stop_via_pidfile` then reads the stale-and-dead pid and takes the
"stale pidfile" branch, returning normally instead of aborting.

**Fix:** test setup/teardown explicitly delete both pidfiles so each test
gets a clean slate. Verified stable across 3 consecutive `rake test`
runs (23 tests, 51 assertions, 0 failures each time).

---

## 5. Graph panel restoration (release-quality requirement)

Disabling the Graph panel as a "TODO for later" was rejected — release
quality includes the visualization layer. Fixing it surfaced two more
PicoRuby:wasm bridge limitations that explain why the original
`GRAPH = JS.global[:ForceGraph3D].new.call(elem)` line had been broken
all along:

1. **Function.prototype.call binding.** `JS::Object#call(arg)` from
   PicoRuby translates to `func.call(arg)` in JS — i.e., binds `arg`
   as `this` instead of passing it positionally. The 3d-force-graph
   factory pattern `ForceGraph3D()(elem)` therefore can't be expressed
   in PicoRuby; the binder receives `this=elem, arg=undefined` and
   crashes inside `r.init` setting innerHTML on undefined. The same
   error reproduced with the unpinned (1.80.0) and pinned (1.73.4)
   versions until the call route was fixed — version pinning alone
   was a red herring.

2. **Float arguments don't cross the bridge.** Even after construction
   was fixed, `GRAPH.linkOpacity(0.4)` raised
   `TypeError: argument must be a String, Integer, or JS::Object`
   because PicoRuby cannot pass Ruby Floats as JS arguments.

Fix: a thin `window.createForceGraph(elem)` helper in `index.erb`
performs the factory invocation **and** all float-bearing configuration
(`nodeAutoColorBy('group').linkOpacity(0.4)`) inside JS, returning a
ready-to-use instance. PicoRuby just calls it via method-call dispatch
with the DOM element. 3d-force-graph is pinned to `1.73.4` (1.80.0 had
its own initialization regression unrelated to the bridge).

Verified with chrome-mcp + live YouTube audio:
- `graph-canvas` innerHTML grows to 457 chars (graph DOM rendered)
- `/api/recent` bootstrap renders 200 perfect entries
- 70 live quick volatile transcripts streamed via WebSocket
- 0 console errors after init

---

## 6. Live WebSocket E2E test (closes the verification gap)

The release-quality scope demands automated coverage of the path that
hid this whole bug — `fluentd → POST /_internal/notify → WebSocket
broadcast → client receives frame`. rack-test only goes as far as
"WEBSOCKETS << fake_socket"; it never exercises the WebSocket
transport.

`test/web/test_live_websocket.rb` (added) does the real thing:

- spawns `puma` as a subprocess on a free port with a clean tmp SQLite
- waits until `/api/recent` responds 2xx
- inside an EventMachine reactor: opens a real
  `Faye::WebSocket::Client` to `/stream`, POSTs
  `{"type":"quick","data":{"text":"PROBE-FROM-TEST"}}` to
  `/_internal/notify`, asserts the broadcast frame arrives at the
  client with the expected type and payload
- tears the puma subprocess down with TERM (KILL fallback at 10s) and
  cleans the tmp dir per test

Run alone: ~0.75s. In `bundle exec rake test`: 24 tests, 54 assertions,
0 failures, stable across 3 consecutive runs.

---

## 7. Final verdict

The core feature
"mic + app audio → 文字化 → SQLite → **Web 表示**"
including its Graph visualization is end-to-end operational, with the
live WebSocket path covered by an automated integration test. The two
PicoRuby:wasm bridge gotchas (Function.prototype.call binding and
no-Float arguments) are documented in source comments where future
contributors will run into them.

### Commits added across this round

- `6813c0b` fix(web): repair PicoRuby:wasm frontend so transcripts render end-to-end
- `64a8688` test: clear stale pidfiles in TestRakeLifecycle setup/teardown
- `eb03281` docs: record 2026-05-06 E5 reverify outcome
- `5a97476` fix(web): pin 3d-force-graph@1.73.4 and restore Graph panel via JS helper
- `636c7a9` test(web): add live-puma + real-WebSocket E2E test for /_internal/notify

### Known open issues (separate scope)

- mini-E5 occasionally needed 2 retries to PASS during Task 4 verification
  (transient timing sensitivity in start:all → afplay → stop:all).
  Single-run-must-pass remains a future hardening target, but this is
  the existing E5 scaffolding's behavior, not regression introduced by
  this work.
- foundation_model_mac records 9 `[warn]` events per long run for
  Apple guardrail-rejected polish requests; raw transcripts still land
  in SQLite. User has accepted as expected upstream behavior.
